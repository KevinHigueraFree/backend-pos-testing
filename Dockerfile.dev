# ============================================
# DOCKERFILE PARA DESARROLLO - CON HOT RELOAD
# ============================================
# Este Dockerfile es para desarrollo, permite hot-reload
# Los cambios en el código se reflejan automáticamente

FROM node:20-alpine

# Establecer el directorio de trabajo
WORKDIR /app

# Copiar solo los archivos de dependencias primero (optimización de cache)
COPY package*.json ./

# Instalar las dependencias
RUN npm install

# NO copiamos el código aquí, se montará como volumen en docker-compose
# Esto permite que los cambios se reflejen sin reconstruir la imagen

# Exponer el puerto
EXPOSE 3001

# Comando por defecto: start:dev tiene watch mode (hot-reload)
CMD ["npm", "run", "start:dev"]

